<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BuJoApp.Xam"
             x:Class="BuJoApp.Xam.MainPage">

    <StackLayout>
        <Label Text="Add a new Page to your Bullet Journal!"
               FontAttributes="Bold"
               FontSize="Large" />
        <Entry HeightRequest="50"
               Placeholder="Title"
               Text="{Binding Title}" />
        <Entry HeightRequest="50"
               Placeholder="Your Content"
               Text="{Binding Content}" />
        <Entry HeightRequest="50"
               Placeholder="Page Number"
               Text="{Binding PageNum}" />
        <Button Command="{Binding AddPageCommand}"
                Text="Add New Page" />
        <ListView ItemsSource="{Binding Pages}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}"
                              Detail="{Binding Content}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
